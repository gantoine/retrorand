<!DOCTYPE html>
<html>
<% include ../partials/head %>

<body>

  <% include ../partials/navbar %>

  <main class="container retroapp">
    <div class="row">
      <div class="col s12 center-align">
        <h1 class="corps-msg white-text">Want to play a retro game, but don't know which one? We can help!</h1>
      </div>
    </div>
    <div class="row content-row main-controls">
      <div class="col s12 m4 l4">
        <div>
          <a class="waves-effect waves-light btn-large yellow darken-2 black-text"
              @click="roll">
            Random
          </a>
        </div>
        <div class="or-divider">
          <span>OR</span>
        </div>
        <div class="input-field">
          <input id="search" type="search" autocomplete="off" v-model="query"
              @keyup.enter="search" @keyup.esc="clear">
          <label for="search">
            <i class="material-icons search">search</i>
          </label>
        </div>
        <div>
          <a class="waves-effect waves-light btn-large green lighten-1 white-text modal-trigger" href="#platforms">Filter Platforms</a>
        </div>
      </div>
      <div class="col s12 m8 l8 results-column">
        <div class="card-panel results grey-text text-darken-4">
          <section v-if="showResult">
            <div class="game-title">{{random.title || info.title}}</div>
            <div v-show="random.platform" transition="fade">
              <a class="game-platform grey-text text-darken-3" @click="library">
                {{info.platform || clean(random.platform)}}
              </a>
            </div>

            <div class="tgdb-results">
              <div class="row">
                <div class="col s6">
                  <img v-show="info.boxart" transition="fade" class="boximage content" v-bind:src="info.boxart"/>
                  <div v-else class="preloader-wrapper big active">
                    <% include ../partials/spinner %>
                  </div>
                </div>
                <div class="col s6">
                  <div v-show="info.id" transition="fade">
                    <div v-show="info.releaseDate">Release Date: {{info.releaseDate}}</div>
                    <div v-show="info.ESRB">ESRB Rating: {{info.ESRB}}</div>
                    <div v-show="info.publisher">Publisher: {{info.publisher}}</div>
                    <div v-show="info.developer">Developer: {{info.developer}}</div>
                    <div v-show="info.players">Players: {{info.players}}</div>
                    <div v-show="info.rating">Rating: {{info.rating}}</div>
                    <ul v-show="info.genres">
                      Genres:
                      <li v-for="genre in info.genres">{{genre}}</li>
                    </ul>
                    <a v-show="info.id" v-bind:href="info.url" target="_blank"
                        class="waves-effect waves-teal btn-flat">
                      View On TheGamesDB
                    </a>
                  </div>
                  <div v-else class="preloader-wrapper big active">
                    <% include ../partials/spinner %>
                  </div>
                </div>
              </div>
            </div>
          </section>

          <section class="search-results" v-if="showFound" transition="fade">
            <ul class="collection with-header" v-for="platform in found">
              <li class="collection-header"><h5>{{clean($key)}}</h5></li>
              <li class="collection-item" v-for="game in platform">
                {{game}}
              </li>
            </ul>
          </section>

          <section class="search-results" v-if="showPlatform" transition="fade">
            <ul class="collection with-header">
              <li class="collection-header"><h5>{{platform}}</h5></li>
              <li class="collection-item" v-for="game in games">
                {{game}}
              </li>
            </ul>
          </section>
        </div>
      </div>
    </div>

    <% include ../partials/platforms %>

  </main>

  <% include ../partials/footer %>

</body>
</html>

<!DOCTYPE html>
<html>
<% include ../partials/head %>

<body>

  <% include ../partials/navbar %>

  <main class="container retroapp">
    <div class="row">
      <div class="col s12 center-align">
        <h1 class="corps-msg white-text">Want to play a retro game, but don't know which one? We can help!</h1>
      </div>
    </div>
    <div class="row content-row">
      <div class="col s12 m4 l4">
        <div>
          <a class="waves-effect waves-light btn-large yellow darken-2 black-text"
              @click="roll">
            Random
          </a>
        </div>
        <div class="or-divider">
          <span>OR</span>
        </div>
        <div class="input-field">
          <input id="search" type="search" autocomplete="off" v-model="query"
              @blur="search" @keyup.enter="search" @keyup.esc="clear">
          <label for="search">
            <i class="material-icons search">search</i>
          </label>
        </div>
        <div>
          <a class="waves-effect waves-light btn-large green lighten-1 white-text modal-trigger" href="#platforms">Filter Platforms</a>
        </div>
      </div>
      <div class="col s12 m8 l8 results-column">
        <div class="card-panel results">
          <section v-if="info || random">
            <div v-show="random.title" v-cloak>
              <h4>{{random.title}}</h5>
              <h5 v-show="!info.platform">{{clean(random.platform)}}</h5>
            </div>
            <div v-show="info.id" v-cloak>
              <div class="row">
                <div class="col s6" v-show="info.boxart">
                  <img v-bind:src="info.boxart"/>
                </div>
                <div class="col s6">
                  <div v-show="info.platform">{{info.platform}}</div>
                  <div v-show="info.releaseDate">Release Date: {{info.releaseDate}}</div>
                  <div v-show="info.ESRB">ESRB Rating: {{info.ESRB}}</div>
                  <div v-show="info.publisher">Publisher: {{info.publisher}}</div>
                  <div v-show="info.developer">Developer: {{info.developer}}</div>
                  <div v-show="info.players">Players: {{info.players}}</div>
                  <div v-show="info.rating">Rating: {{info.rating}}</div>
                  <ul v-show="info.genres">
                    Genres:
                    <li v-for="genre in info.genres">{{genre}}</li>
                  </ul>
                  <a v-show="info.id" v-bind:href="info.url"
                      class="waves-effect waves-teal btn-flat">
                    View On TheGamesDB
                  </a>
                </div>
              </div>
            </div>
          </section>
          <section class="search-results" v-if="found">
            <ul class="collection with-header" v-for="platform in found">
              <li class="collection-header"><h5>{{clean($key)}}</h5></li>
              <li class="collection-item" v-for="game in platform">
                {{game}}
              </li>
            </ul>
          </section>
        </div>
      </div>
    </div>
  </main>

  <div id="platforms" class="modal bottom-sheet">
    <div class="modal-content">
      <div class="row">
        <div class="col s2 m6 l6">
          <h4>Platforms</h4>
        </div>
        <div class="col s2 m2 l2">
          <a class="waves-effect waves-light btn yellow darken-2 black-text">Select All</a>
        </div>
        <div class="col s8 m4 l4">
          <div class="input-field">
            <input id="platform-search" type="search" autocomplete="off">
            <label for="platform-search">
              <i class="material-icons search">search</i>
            </label>
          </div>
        </div>
      </div>
      <% include ../partials/platforms %>
    </div>
    <div class="modal-footer">
      <a href="#!" class="modal-action modal-close waves-effect waves-light btn-flat">Done</a>
    </div>
  </div>

  <% include ../partials/footer %>

</body>
</html>
